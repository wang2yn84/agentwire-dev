# Removed 2026-04-26 — Phase 2D follow-up flag flip.
#
# This file used to test the AGENTWIRE_REPL_TUI dispatch fork. The fork is
# gone: `run_repl` now calls `run_textual_repl` directly for interactive
# sessions. Print mode (`-p PROMPT`) keeps the stdout single-shot path.
#
# Replaced by tests in test_repl_textual_app.py and test_repl_sdk.py
# (TestPrintModeIntegration).
